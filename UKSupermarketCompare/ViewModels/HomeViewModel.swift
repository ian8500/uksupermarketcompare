import Foundation

final class HomeViewModel: ObservableObject {
    let title = "Shop smarter, save more each week"
    let subtitle = "UKSupermarketCompare helps you build a basket, compare prices, and reuse your weekly flow in seconds."
}
